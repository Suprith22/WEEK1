#include <stdio.h>
int main() {
 int a,b;
 printf("Enter two numbers");
 scanf("%d%d",&a,&b);
 printf("The sum,difference,product and div of the two numbers is %d,%d,%d,%d",a+b,a-b,a*b,a/b);
 return 0;
 }
